model:
  name: Qwen/Qwen2.5-VL-7B-Instruct
  dtype: bfloat16
  attn_implementation: sdpa
  min_pixels: 200704
  max_pixels: 1003520
  max_new_tokens: 320
  do_sample: false

runtime:
  backend: openai_compatible
  base_url: http://127.0.0.1:8000/v1
  request_timeout_seconds: 300
  max_retries: 0
  retry_backoff_seconds: 2
  verify_tls: true
  api_key_env: DMS_MODEL_API_KEY
  reported_peak_gpu_memory_gib: 0.0

generation:
  temperature: null
  top_p: null

source_notes:
  output_cap: "Frozen at 320 tokens to reduce truncated CodeAct calls in the 7B engineering variant."
  transport: "Connection: close; request-level retries remain disabled so the task-level infrastructure retry is auditable."
